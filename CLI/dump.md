gcloud compute project-info describe --project [PROJECT_ID]

1. What if you just want to change configuration for a single command and don't want to have to activate that configuration? For example, the command to create a GCE instance is

$ > gcloud compute instances create [INSTANCE_NAME]